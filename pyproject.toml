[project]
name = "Vadathon"
version = "0.1.0"
description = "Creating a Hackathon in India!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "python-dotenv",
    "requests",
]
